# CLAUDE.md

このファイルは、このリポジトリで Claude Code が作業する際のガイドラインです。

## プロジェクト概要

不動産関連のアプリケーション（realestate-app）。
現時点ではプロジェクトは初期状態です。技術スタックや構成が決まり次第、このセクションを更新してください。

## デプロイ情報

- 本番URL：https://プロジェクト名.vercel.app
- Supabaseプロジェクト名：realestate-app

## Git 運用ルール

**コードを変更するたびに、必ず GitHub へプッシュすること。**

- 意味のある単位（1機能・1修正など）でこまめにコミットする。
- コミット後は必ず `git push` してリモート（GitHub）に反映する。ローカルにコミットしたまま放置しない。
- コミットメッセージは変更内容が分かる簡潔な日本語または英語で書き、「なぜ」変更したかを一言添える。
- 作業前に `git status` で状態を確認し、意図しない変更や未コミットの作業を上書きしないようにする。
- force push（`git push --force` など）や `git reset --hard`、履歴を書き換える操作は、ユーザーの明示的な許可なく行わない。
- 認証情報や `.env` などの秘匿情報を誤ってコミット・プッシュしないよう、コミット前に差分を確認する。
- リモートリポジトリ（GitHub）が未設定の場合は、先にユーザーへリポジトリURLを確認してから `git remote add origin <URL>` を設定する。

## 開発時の注意事項

- 新しいライブラリやフレームワークを導入する際は、必要最小限の変更にとどめる。
- 変更後は可能な範囲でビルド・テスト・動作確認を行ってから、コミット・プッシュする。
